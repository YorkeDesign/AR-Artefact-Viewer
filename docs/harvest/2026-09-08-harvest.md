# Harvest ingest - 2026-09-08

Assessed 8 captures (oldest first) of 50 awaiting a verdict. A ninth was
pulled from the queue but could not be read, carries no verdict, and is
recorded below. **42 remain queued**, including that one.

Approval mode: `gated`. No proposals this run, and nothing implemented.

**No proposal is the honest result here.** This repo's live interests are
model-viewer and Quick Look capabilities, in-app browser workarounds, and CDN
delivery of GLB and USDZ. Every capture in this batch is upstream of that: how
an artefact gets scanned or authored, not how the viewer serves it. Manufacturing
a proposal out of any of them would be dishonest.

- `c7d7a97d` | KIRI 3DGS-to-Mesh 3.0 reconstructing thin surfaces, down to flower petals | NOTE | changes which artefacts are scannable at all, which is upstream of this repo; the viewer is unaffected either way | capture only (WEAK)
- `0ca05236` | The same 3.0 release, with detail on cables, branches and chair legs surviving conversion | NOTE | same capability, more evidence; still upstream of the viewer | capture only (WEAK)
- `84a6f557` | Apple plus AI Gaussian splat generation, meshes still easier to integrate | NO-OP | model-viewer renders meshes, not splats; the post says nothing about the mesh path we use | capture only (WEAK)
- `8f176dc3` | Text-prompt to 3D particle physics, emitting React and Three.js code | NO-OP | this viewer is model-viewer, not Three.js or React; no code here to receive it | capture only (WEAK)
- `304b9ebd` | Blender addon applying full PBR via the I2M model, 4K albedo, roughness, metallic, IoR | NOTE | PBR quality is what makes a scanned artefact read as real in AR, so this is worth knowing for the authoring step; it is a Blender desktop workflow, nothing the viewer runs | capture only (WEAK)
- `77e7cb12` | Semantic search inside a 3D Gaussian splat scene | NO-OP | scene-scale search; this repo serves one artefact at a time as GLB or USDZ | capture only (WEAK)
- `8dc3f84e` | q3d, a VS Code point cloud viewer for PCD, PLY, LAS, LAZ, E57 | NO-OP | a desktop developer tool for point clouds; this repo ships meshes to phones | capture only (WEAK)
- `4a7e5ab1` | RT-Splatting (CVPR 2026 Highlight, MIT): joint reflection and transmission for semi-transparent specular surfaces | NOTE | the strongest artifact in this batch and squarely on a real problem, since glass and glazed ceramic artefacts are exactly this case; it is a splat renderer though, and the pipeline here is mesh-based, so it only becomes actionable if capture ever moves to splats | https://arxiv.org/abs/2605.18263 and https://github.com/sjj118/RT-Splatting (STRONG)

## Item that could not be read

`e25bbbb0-1dc8-4a2e-a49f-dbeae3a00781` (Apple LiTo, interactive image-to-3D)
appeared in the collection queue but `get_item` returned "item not found". No
verdict was recorded, because recording one for a capture nobody could read is
exactly the failure this process exists to prevent. It stays in the queue and
should be retried next run; if it fails again the vault has a dangling queue
row worth investigating.

## Note on authority

Eight captures resolve only back to X and are rated WEAK. One resolves to an
arXiv paper and an MIT-licensed repository, and is rated STRONG.
