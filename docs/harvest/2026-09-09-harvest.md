# Harvest ingest - 2026-09-09

Assessed 10 of 44 captures awaiting a verdict (oldest first). 34 remain queued.

Approval mode: `gated`. **No proposals this run**, and that is the finding:
all ten captures are generative-3D and rendering news. None of them touch what
this project actually is, which is a delivery problem: model-viewer behaviour,
Quick Look and Scene Viewer limits, in-app browsers that block AR, and getting
files down a CDN onto a phone.

- `e25bbbb0` | LiTo (ICLR 2026), Apple's open-sourced interactive image-to-3D generation | NOTE | the nearest thing here to the photogrammetry interest, and it is open source from a named lab, but it makes models rather than delivering them. Nothing in this repo produces geometry | capture only (WEAK)
- `984542cf` | Pi3X versus DepthAnythingV2, how far image-to-depth models have come | NO-OP | scan-capture quality, and this repo does not capture. It serves files someone else scanned | capture only (WEAK)
- `c214774b` | Pixal3D (SIGGRAPH 2026), pixel-aligned image-to-3D generation | NO-OP | asset generation research; no AR delivery surface | capture only (WEAK)
- `d487d058` | Nanite-style renderer for Three.js, ~100 million triangles in a browser | NOTE | impressive, and irrelevant to the actual bottleneck: on iOS the model is handed to Quick Look, which is Apple's renderer, not ours. A custom renderer cannot help the AR path, only a non-AR preview | capture only (WEAK)
- `bf4a0abc` | dgreenheck/webgpu-claude-skill | ELSEWHERE | a Claude Code skill, so it belongs to Yorke-Claude-Setup | https://github.com/dgreenheck/webgpu-claude-skill (MEDIUM)
- `8cefb1ef` | Reply pointing at the sea3d format file in the three.js repo | NO-OP | a pointer inside someone else's thread; no artefact and no decision here | capture only (WEAK)
- `4bdeb255` | GenRecon project page: multi-view scene reconstruction with a generative prior | NOTE | genuinely on the scanning interest, and worth knowing when the question is how to capture an artefact well. Not adoptable by a viewer that only serves finished GLB and USDZ | https://kasothaphie.github.io/GenRecon/ (MEDIUM)
- `7edf52be` | One-click AI texturing in a web 3D editor | NO-OP | authoring feature in another product; nothing here textures | capture only (WEAK)
- `67655ff6` | CozyBlanket Pro, AI-assisted retopology and mesh cleanup toolkit | NOTE | file size is a real constraint on this project, and retopology is one lever on it, but this is a commercial desktop app on the authoring side of the line, not something the viewer can use | capture only (WEAK)
- `049fd58a` | Every Hugging Face Space is an API your agents can call | NO-OP | agent tooling tip, unrelated to WebAR delivery | capture only (WEAK)

## What this queue is missing

Ten captures, zero that inform the questions this repo has: whether
model-viewer's newer releases change the in-app browser workaround, what Quick
Look now accepts, and whether serving USDZ from GitHub raw is still the right
delivery path. Worth noting for the vault side rather than fixing here: the
tags that pull items to this project are matching on "3D" rather than on AR
delivery.
